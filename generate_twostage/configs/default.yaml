dataset: PACS
image_dir: ./PACS
generative_model: SDXL
start_class: 6
end_class: 6

api_key: none # For Dalle2

prompt_dir: './prompts/totalprompts_PACS.json'
num_samples_path: none # txt file to run conveniently


# If you want to manually set the number of samples of each class...
increase_ratio: 1.2 # MA * increase_ratio
# num_samples_per_cls: 500 # Fix the number of samples per cls
# num_samples:
#   person: 100
#   horse: 100
